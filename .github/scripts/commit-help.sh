#!/bin/bash
# Quick reference card for conventional commits

cat << 'EOF'
╔════════════════════════════════════════════════════════════════╗
║           📋 CONVENTIONAL COMMITS QUICK REFERENCE              ║
╚════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│ FEATURES (Minor Version Bump: 1.0.0 → 1.1.0)                  │
└────────────────────────────────────────────────────────────────┘
  git commit -m "feat: add new user dashboard"
  git commit -m "feat(auth): implement OAuth2 login"
  git commit -m "feat(api): add export endpoint"

┌────────────────────────────────────────────────────────────────┐
│ BUG FIXES (Patch Version Bump: 1.0.0 → 1.0.1)                 │
└────────────────────────────────────────────────────────────────┘
  git commit -m "fix: resolve login timeout issue"
  git commit -m "fix(ui): correct button styling"
  git commit -m "fix(api): handle null responses"

┌────────────────────────────────────────────────────────────────┐
│ BREAKING CHANGES (Major Version Bump: 1.0.0 → 2.0.0) ⚠️       │
└────────────────────────────────────────────────────────────────┘
  git commit -m "feat!: redesign API structure"
  
  git commit -m "feat: change auth system
  
  BREAKING CHANGE: Old tokens no longer supported"

┌────────────────────────────────────────────────────────────────┐
│ PERFORMANCE (Patch Version Bump)                               │
└────────────────────────────────────────────────────────────────┘
  git commit -m "perf: optimize database queries"
  git commit -m "perf(api): reduce response time"

┌────────────────────────────────────────────────────────────────┐
│ REFACTORING (Patch Version Bump)                               │
└────────────────────────────────────────────────────────────────┘
  git commit -m "refactor: simplify authentication logic"
  git commit -m "refactor(ui): restructure components"

┌────────────────────────────────────────────────────────────────┐
│ NO VERSION BUMP (Documentation, Tests, Chores)                 │
└────────────────────────────────────────────────────────────────┘
  git commit -m "docs: update installation guide"
  git commit -m "test: add unit tests for auth"
  git commit -m "chore: update dependencies"
  git commit -m "style: fix code formatting"

┌────────────────────────────────────────────────────────────────┐
│ SKIP RELEASE                                                    │
└────────────────────────────────────────────────────────────────┘
  git commit -m "chore: minor updates [skip release]"
  git commit -m "docs: fix typo [skip ci]"

╔════════════════════════════════════════════════════════════════╗
║                      💡 BEST PRACTICES                         ║
╚════════════════════════════════════════════════════════════════╝

✓ Use present tense: "add feature" not "added feature"
✓ Keep first line under 72 characters
✓ Add scope in parentheses: feat(api): ...
✓ Be specific and descriptive
✓ Reference issues: fix: resolve #123

✗ Don't use vague messages: "fix stuff", "updates", "WIP"
✗ Don't mix multiple changes in one commit

╔════════════════════════════════════════════════════════════════╗
║                      🚀 WORKFLOW                               ║
╚════════════════════════════════════════════════════════════════╝

1. Make changes to your code
2. Stage files: git add .
3. Commit with conventional format: git commit -m "feat: ..."
4. Push to main: git push origin main
5. ✨ Auto-release happens automatically!

╔════════════════════════════════════════════════════════════════╗
║                   📚 MORE INFORMATION                          ║
╚════════════════════════════════════════════════════════════════╝

Full Guide: docs/AUTO_RELEASE_GUIDE.md
Specification: https://www.conventionalcommits.org/

EOF
