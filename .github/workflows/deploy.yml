name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v3
      
      - name: 🔍 Debug - Show server value (first 10 chars)
        run: |
          SERVER="${{ secrets.FTP_SERVER }}"
          echo "Server length: ${#SERVER}"
          echo "First 10 chars: ${SERVER:0:10}"
          echo "Last char code: $(printf '%d' "'${SERVER: -1}")"